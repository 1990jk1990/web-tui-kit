# web-tui-kit

A reusable web UI design system inspired by Debian debconf, `dialog`, and `whiptail`.

The goal is to provide one canonical UI reference that can be reused across browser-based applications on Linux desktops and Android devices.

## Status

Initial design-system scaffold in progress.
