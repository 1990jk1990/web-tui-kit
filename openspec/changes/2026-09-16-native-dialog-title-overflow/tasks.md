# Tasks

- [x] Record downstream defect and classify it as a compatible `.tui-dialog` presentation fix.
- [ ] Set canonical `.tui-dialog` overflow to visible in `src/tui.css`.
- [ ] Add native `<dialog>` regression coverage to the cross-browser interaction fixture and runner.
- [ ] Add structural safeguards for the new browser regression case.
- [ ] Reconcile the canonical `web-tui-kit` specification and practical design-system guidance.
- [ ] Add an Unreleased changelog entry without changing `VERSION`.
- [ ] Run structural, AI-DOC-1, strict docs, visual, interaction, accessibility, framework-recipe, and release-archive checks through pull-request CI where applicable.
