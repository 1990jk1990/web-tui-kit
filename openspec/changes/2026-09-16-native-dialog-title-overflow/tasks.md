# Tasks

- [x] Record downstream defect and classify it as a compatible `.tui-dialog` presentation fix.
- [x] Set canonical `.tui-dialog` overflow to visible in `src/tui.css`.
- [x] Add native `<dialog>` regression coverage to the cross-browser interaction fixture and runner.
- [x] Add structural safeguards for the new browser regression case.
- [x] Reconcile the canonical `web-tui-kit` and browser-verification specifications plus practical design-system guidance.
- [x] Add an Unreleased changelog entry without changing `VERSION`.
- [ ] Run structural, AI-DOC-1, strict docs, visual, interaction, accessibility, framework-recipe, and release-archive checks through pull-request CI where applicable.
